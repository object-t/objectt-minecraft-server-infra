

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: minecraft-network
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/object-t/objectt-minecraft-server-infra.git
      revision: HEAD
      directories:
      - path: k8s-manifests/overlays/*
  template:
    metadata:
      name: objectt-minecraft-{{path.basename}}
    spec:
      project: default
      source:
        repoURL: https://github.com/object-t/objectt-minecraft-server-infra.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: objectt-minecraft-{{path.basename}}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true